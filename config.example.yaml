# Agent Memory MCP Server Configuration
# All values shown are defaults. Uncomment to customize.

# storage:
#   db_path: ~/.agent-memory/memory.db

# embedding:
#   provider: openai          # openai | ollama | none
#   base_url: https://api.openai.com/v1
#   api_key: ${OPENAI_API_KEY}
#   model: text-embedding-3-small
#   dimensions: 1536

# consolidation:
#   provider: openai          # openai | ollama | none
#   base_url: https://api.openai.com/v1
#   api_key: ${OPENAI_API_KEY}
#   model: gpt-4o-mini
#   auto_interval_minutes: 0  # 0 = disabled
#   min_memories: 3

# server:
#   transport: stdio           # stdio | http
#   http_host: 127.0.0.1
#   http_port: 8888
#   auth_token: ${AGENT_MEMORY_AUTH_TOKEN}
#   cors_origins:
#     - "http://localhost:*"
#   tls_cert:                      # path to TLS certificate (for Tailscale HTTPS)
#   tls_key:                       # path to TLS private key

# ingestion:
#   allowed_paths: []          # empty = ingest_file disabled
#   max_file_size_mb: 1
#   supported_extensions:
#     - .txt
#     - .md
#     - .json
#     - .csv
#     - .yaml
#     - .yml
#     - .xml
#     - .log

# vault:
#   enabled: false
#   vault_path: ~/Vault             # Obsidian vault root
#   sync_folder: memory-vault       # subfolder within vault
#   watch_local: false              # watch for direct .md edits (Mac Mini only)
#   write_on_store: true            # auto-write .md when memory stored via MCP
#   api_port: 8889                  # vault REST API port (separate from MCP)

# log_level: INFO
